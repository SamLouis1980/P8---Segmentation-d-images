version: "3.8"

services:
  segmentation-service:
    container_name: segmentation-container
    image: p8-segmentation-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always

volumes:
  - "./cle_gcp.json:/app/cle_gcp.json"
